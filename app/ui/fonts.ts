import { Roboto, Inter, Lobster } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const roboto = Roboto({ subsets: ['latin'], weight: '400' });
export const lobster = Lobster({ subsets: ['latin'], weight: '400' });
